name: Validate plugin

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Install the Claude Code validator
        run: npm install --global @anthropic-ai/claude-code@2.1.270
      - name: Validate manifests and components
        run: |
          claude plugin validate .claude-plugin/plugin.json --strict
          claude plugin validate .claude-plugin/marketplace.json --strict
          claude plugin validate skills --strict
          claude plugin validate agents --strict
          python3 scripts/build_desktop.py --check
          claude plugin validate desktop --strict
          claude plugin validate desktop/skills --strict
          claude plugin validate desktop/agents --strict
      - name: Test the independent connection with synthetic OAuth credentials
        run: python3 scripts/test_desktop_connection.py
      - name: Build and validate the distribution
        run: |
          python3 scripts/package_plugin.py
          python3 -m zipfile -e dist/adzviser-[0-9]*.zip /tmp/adzviser-distribution
          claude plugin validate /tmp/adzviser-distribution --strict
          python3 scripts/package_plugin.py --edition desktop
          python3 -m zipfile -e dist/adzviser-desktop-*.zip /tmp/adzviser-desktop-distribution
          claude plugin validate /tmp/adzviser-desktop-distribution --strict
